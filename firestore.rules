rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Per-language translation caches are server-managed. Clients access them
    // only through the callable function, whose Admin SDK bypasses these rules.
    match /questionTranslations/{questionId} {
      allow read, write: if false;
    }
  }
}
